SELECT
  ID,
  firstName as FIRST_NAME,
  lastName as LAST_NAME,
  dateOfBirth,
  telephone,
  address,
  city,
  postal_code,
  provinceID,
  email,
  medicareNumber,
  citizenship
FROM
  Employee P
WHERE ID = ?
